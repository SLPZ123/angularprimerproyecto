import { BooleanEstiloDirective } from './boolean-estilo.directive';

describe('CabeceraLetraDirective', () => {
  it('should create an instance', () => {
    //const directive = new CabeceraLetraDirective();
   // expect(directive).toBeTruthy();
  });
});